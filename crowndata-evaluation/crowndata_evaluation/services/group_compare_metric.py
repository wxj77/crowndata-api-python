from typing import Any


def get_similarity_score(
    data1: Any = None,
    data2: Any = None,
) -> float:
    # Placeholder logic for calculating action consistency
    # You can replace this with the actual calculation logic
    if (data1 is not None) and (data2 is not None):
        return 0.24  # Example action consistency for 'data'
    return 0.0
